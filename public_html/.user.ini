; Hostinger shared hosting reads .user.ini automatically (PHP-FPM). Adjust to the
; largest video size you expect; some shared plans cap this lower regardless — check hPanel.
upload_max_filesize = 512M
post_max_size = 520M
max_execution_time = 300
max_input_time = 300
memory_limit = 256M
